
export const snippetTypes = ["Headline", "Social Media Post (Short)", "Email Subject Line"] as const;

// Optional: Define the derived type if it's useful elsewhere, though not strictly needed for this fix.
// export type SnippetType = typeof snippetTypes[number];
